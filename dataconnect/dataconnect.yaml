specVersion: "v1"
serviceId: "studio"
location: "us-central1"
# The schema section is required for DataConnect to function.
# If you remove it, you will encounter errors.
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "studio-fdc"
connectorDirs: ["./connector"]
